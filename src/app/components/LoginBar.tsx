const LoginBar = () => {
  return (
    <div className="flex justify-end">
      <button className="bg-[#FFA33C] hover:bg-orange-500 text-sm font-bold py-2 px-4 rounded">
        로그인
      </button>
    </div>
  );
};

export default LoginBar;
